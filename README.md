# Prometheus node_exporter Install Script
Setup for Prometheus Node Exporter

## Usage

Install the node_exporter by running this command:

```
start.sh
```

To remove the node_exporter from the system:

```
stop.sh
```